# 2026-09-24

## tower-mcp 代码评审（对照 docs.tower.im 官方文档）

- 项目：`/Users/mk/WorkBuddy AI/2026-09-24-10-13-19/tower-mcp`，TypeScript + @modelcontextprotocol/sdk 1.30，54 个工具，18 个测试全过，构建无告警。
- 官方文档源：github.com/mycolorway/tower-api-document（docsify，docs/api/*.md），已下载到 /tmp/tower-docs/ 逐模块比对端点与请求体。
- 结论：54 个端点路径、方法、请求体结构全部与官方文档一致（含 topic attfile_guids 顶层、todo 自定义字段并入 todo 对象、cc_members 的 guids 包裹、time_log 字段名 starts_at/ends_at）。
- 发现的问题（按优先级）：
  - P0-1 刷新令牌缺 `Authorization: Bearer` 头（tower-client.ts doRefresh，官方文档刷新接口明确要求该头）。
  - P0-2 刷新令牌未默认带 redirect_uri，且 auth-cli 未把授权时实际用的 redirect_uri 持久化进 token.json（文档要求与授权时一致）。
  - P0-3 `tower_get_topic` 正文 content 未做 HTML→纯文本清洗（jsonapi.ts flattenResource 只清洗 desc 和 comments.content），与 index.ts INSTRUCTIONS / README 的声明不符。
  - P1 分页信息丢失：Tower 响应的 links（含 next）在 flattenDocument 中被丢弃，模型无法翻页；tower_list_topics / tower_list_uploads 未暴露 page 参数（文档 links 显示支持）。
  - P1 OSS 直传 Blob 未设 Content-Type，mimeType 回调为 octet-stream，影响 Tower 端图片预览。
  - P2：compact() 递归删空数组有语义歧义；token 文件已存在时 0600 不收紧；.env.example 的 TOWER_TOKEN_FILE 示例与 README 默认值不一致；attfiles 的 content_type 参数未暴露。
- 尚未修复，等用户确认后逐项修。

## P0 修复（已完成）

- P0-1 `tower-client.ts` doRefresh：刷新请求补上官方要求的 `Authorization: Bearer <access_token>` 头（有旧 token 时才带）。
- P0-2 redirect_uri 链路：StoredToken 新增 `redirectUri` 字段并随令牌文件持久化；`applyTokenResponse(body, redirectUri?)` 在授权换令牌时记录实际回调地址；doRefresh 默认取 `TOWER_REDIRECT_URI ?? token.redirectUri`；`describeAuthState` 暴露生效的 redirectUri 便于诊断。
- P0-3 `jsonapi.ts` flattenResource：`content` 富文本清洗范围从 `comments` 扩展到 `comments + topics`，讨论正文不再返回原始 HTML。
- 配套：新增 4 个测试（刷新带 Bearer 头+文件 redirect_uri、env 覆盖优先级、授权后 redirect_uri 持久化、topics 正文清洗），22/22 全过；README 配置项表与「令牌刷新」小节同步更新。
- P1（分页 links 丢失、OSS 直传 Content-Type）与 P2 项仍未处理。

## P1 修复（已完成）

- P1-4 分页：`jsonapi.ts` flattenDocument 数组分支——响应带 `links.next` 时包装成 `{ items, has_more: true, next_page }`（next_page 从 links.next 的 page[number] 解析），无 next 时保持纯数组（兼容现有行为）；`tower_list_topics` / `tower_list_uploads` 补上 page 参数；README 新增「分页」小节。
- P1-5 OSS 直传：`upload.ts` 新增 `guessMimeType`（30+ 常见扩展名映射，未知回退 octet-stream），Blob 设置 type，OSS 对象 Content-Type 不再是 octet-stream，图片预览可正常生成。
- 新增 5 个测试（links.next 包装、无 next 保持数组、MIME 常见映射、回退、隐藏文件），27/27 全过；README 测试数与覆盖说明同步为 27。
- P2 项（compact 空数组语义、token 文件权限收紧、.env.example 路径不一致、attfiles content_type 参数）仍未处理。

## P2 修复（已完成，评审闭环）

- compact() 不再删除空数组（空数组有语义：comments: [] = 没有评论），只去 undefined；对应测试更新。
- saveToken 写入后 chmodSync 0o600 强制收紧权限（mode 参数只对新建文件生效）；exchange 测试里预置 0o666 文件验证收紧。
- .env.example 的 TOWER_TOKEN_FILE 示例改为 ~/.tower-mcp/token.json，与 README 默认值一致。
- tower_get_attachment_url 补上 content_type 参数（官方文档 attfiles 接口的可选项）。
- 27/27 测试全过。P0/P1/P2 全部修复完毕，项目待实际授权联调验证（建议重跑 npm run auth 让 redirectUri 落盘）。

## 下午：README 结构调整（非 WorkBuddy 客户端接入说明独立成节）

- 背景：仓库在上午评审后又演进了（一键安装 install.sh、启动器 bin/tower-mcp、凭证 ~/.tower-mcp/env、删除 confirm 硬约束、40 个测试）。install.sh 的 TOWER_MCP_CONFIG 默认写 WorkBuddy 的 ~/.workbuddy-ai/mcp.json，其他客户端（Claude Desktop/Cursor/Cline）的接入说明埋在快速开始第 4 步里不显眼。
- 改动：新增顶级章节「## 接入其他 MCP 客户端（Claude Desktop / Cursor / Cline 等）」，含各客户端配置文件路径表、install.sh + TOWER_MCP_CONFIG 注册方式、print-config 手动粘贴方式、重启提醒；一键安装节与快速开始第 4 步都加了跳转链接。
- 核实过 install.sh 只支持 --no-register / --uninstall / --help，文档中不能写不存在的 flag。
- 注意：register-mcp.mjs 用 TOWER_MCP_CONFIG + TOWER_MCP_NAME 环境变量；凭证统一在 ~/.tower-mcp/env，多客户端共享一份安装。
